{
  "name": "multisig-wallet",
  "version": "1.0.0",
  "description": "Smart contract which will allow the transaction only after the set approvers have approved",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "skiran017",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/test-helpers": "^0.5.15",
    "@truffle/hdwallet-provider": "^2.0.1"
  }
}
